
document.addEventListener("DOMContentLoaded", function () {
    const btnPost = document.getElementById("submit__button");

    btnPost.addEventListener("click", (e)=> {
        e.preventDefault()

        
    });
});